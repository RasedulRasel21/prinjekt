{{ 'mass-production-accordion.css' | asset_url | stylesheet_tag }}

<div class="mass-production-section">
  <div class="container page-width">
    {% if section.settings.title != blank %}
      <h2 class="section-title">{{ section.settings.title }}</h2>
    {% endif %}
    
    {% if section.settings.description != blank %}
      <p class="section-description">{{ section.settings.description }}</p>
    {% endif %}

    <div class="accordion-wrapper">
      {% for block in section.blocks %}
        <div class="accordion-item" {{ block.shopify_attributes }}>
          <button class="accordion-header" type="button" aria-expanded="false">
            <span class="accordion-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                <circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2"/>
                <path d="M12 8v8M8 12h8" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
              </svg>
            </span>
            <span class="accordion-title">{{ block.settings.accordion_title }}</span>
          </button>
          
          <div class="accordion-content">
            <div class="accordion-inner">
              <div class="content-left">
                <div class="specifications">
                  {% if block.settings.spec_1_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_1_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_1_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_2_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_2_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_2_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_3_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_3_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_3_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_4_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_4_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_4_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_5_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_5_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_5_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_6_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_6_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_6_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_7_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_7_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_7_value }}</span>
                    </div>
                  {% endif %}
                  
                  {% if block.settings.spec_8_label != blank %}
                    <div class="spec-row">
                      <span class="spec-label">{{ block.settings.spec_8_label }}</span>
                      <span class="spec-value">{{ block.settings.spec_8_value }}</span>
                    </div>
                  {% endif %}
                </div>
                
                {% if block.settings.button_text != blank %}
                  <a href="{{ block.settings.button_link }}" class="cta-button">
                    {{ block.settings.button_text }}
                  </a>
                {% endif %}
              </div>
              
              <div class="content-right">
                {% if block.settings.image != blank %}
                  <img 
                    src="{{ block.settings.image | img_url: '800x' }}" 
                    alt="{{ block.settings.image.alt | escape }}"
                    loading="lazy"
                    width="800"
                    height="auto"
                  >
                {% else %}
                  <div class="placeholder-image">
                    {{ 'image' | placeholder_svg_tag }}
                  </div>
                {% endif %}
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const accordionHeaders = document.querySelectorAll('.accordion-header');
    
    accordionHeaders.forEach(header => {
      header.addEventListener('click', function() {
        const accordionItem = this.closest('.accordion-item');
        const content = accordionItem.querySelector('.accordion-content');
        const isExpanded = this.getAttribute('aria-expanded') === 'true';
        
        // Close all other accordions
        document.querySelectorAll('.accordion-item').forEach(item => {
          if (item !== accordionItem) {
            item.classList.remove('active');
            item.querySelector('.accordion-header').setAttribute('aria-expanded', 'false');
            item.querySelector('.accordion-content').style.maxHeight = null;
          }
        });
        
        // Toggle current accordion
        if (isExpanded) {
          accordionItem.classList.remove('active');
          this.setAttribute('aria-expanded', 'false');
          content.style.maxHeight = null;
        } else {
          accordionItem.classList.add('active');
          this.setAttribute('aria-expanded', 'true');
          content.style.maxHeight = content.scrollHeight + 'px';
        }
      });
    });
  });
</script>

{% schema %}
{
  "name": "Mass Production Accordion",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Individuelle Angebote für die Massenfertigung"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Section Description",
      "default": "Wir sind Ihre Anlaufstelle für alles rund um das faszinierende Thema 3D-Druck..."
    }
  ],
  "blocks": [
    {
      "type": "accordion_item",
      "name": "Accordion Item",
      "settings": [
        {
          "type": "text",
          "id": "accordion_title",
          "label": "Accordion Title",
          "default": "Kleine Teile"
        },
        {
          "type": "text",
          "id": "spec_1_label",
          "label": "Specification 1 Label",
          "default": "Bestellmenge:"
        },
        {
          "type": "text",
          "id": "spec_1_value",
          "label": "Specification 1 Value",
          "default": "10.000 Stk."
        },
        {
          "type": "text",
          "id": "spec_2_label",
          "label": "Specification 2 Label",
          "default": "Material:"
        },
        {
          "type": "text",
          "id": "spec_2_value",
          "label": "Specification 2 Value",
          "default": "PETG Gold"
        },
        {
          "type": "text",
          "id": "spec_3_label",
          "label": "Specification 3 Label",
          "default": "3D Druck:"
        },
        {
          "type": "text",
          "id": "spec_3_value",
          "label": "Specification 3 Value",
          "default": "0,8€"
        },
        {
          "type": "text",
          "id": "spec_4_label",
          "label": "Specification 4 Label",
          "default": "Zusammenbau:"
        },
        {
          "type": "text",
          "id": "spec_4_value",
          "label": "Specification 4 Value",
          "default": "1,5€/Stk."
        },
        {
          "type": "text",
          "id": "spec_5_label",
          "label": "Specification 5 Label",
          "default": "Druckgewicht:"
        },
        {
          "type": "text",
          "id": "spec_5_value",
          "label": "Specification 5 Value",
          "default": "8g."
        },
        {
          "type": "text",
          "id": "spec_6_label",
          "label": "Specification 6 Label",
          "default": "Gesamte Druckzeit:"
        },
        {
          "type": "text",
          "id": "spec_6_value",
          "label": "Specification 6 Value",
          "default": "3000 Stunden / 125 Tage"
        },
        {
          "type": "text",
          "id": "spec_7_label",
          "label": "Specification 7 Label",
          "default": "Preis Pro Gramm:"
        },
        {
          "type": "text",
          "id": "spec_7_value",
          "label": "Specification 7 Value",
          "default": "0,1€"
        },
        {
          "type": "text",
          "id": "spec_8_label",
          "label": "Specification 8 Label",
          "default": "Lieferzeit:"
        },
        {
          "type": "text",
          "id": "spec_8_value",
          "label": "Specification 8 Value",
          "default": "7 Tage"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button Text",
          "default": "Jetzt 3D-Druck bestellen"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Mass Production Accordion",
      "blocks": [
        {
          "type": "accordion_item"
        },
        {
          "type": "accordion_item",
          "settings": {
            "accordion_title": "Große Teile"
          }
        }
      ]
    }
  ]
}
{% endschema %}