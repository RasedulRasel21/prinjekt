{% comment %}
  Section: 3D Printing Methods
  Description: Displays printing methods with cards
{% endcomment %}

{{ 'printing-methods.css' | asset_url | stylesheet_tag }}

<div class="printing-methods-section">
  <div class="printing-methods-container page-width">
    <div class="printing-methods-content">
      <!-- Left Side: Title and Description -->
      <div class="printing-methods-left">
        {% if section.settings.title != blank %}
          <h2 class="printing-methods-title">{{ section.settings.title }}</h2>
        {% endif %}
        {% if section.settings.description != blank %}
          <div class="printing-methods-description">
            {{ section.settings.description }}
          </div>
        {% endif %}
      </div>

      <!-- Right Side: Cards -->
      <div class="printing-methods-right">
        {% for block in section.blocks %}
          <div class="printing-method-card" {{ block.shopify_attributes }}>
            <h3 class="card-title">{{ block.settings.card_title }}</h3>
            <p class="card-description">{{ block.settings.card_description }}</p>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "3D Printing Methods",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "Wie drucken wir eigentlich?"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Section Description",
      "default": "<p>Es gibt eine Mehrzahl an möglichen 3D-Druckverfahren. Wichtig ist, dass man sich für die Variante entscheidet, die am besten für ihr Projekt geeignet ist oder lassen Sie sich von uns beraten.</p>"
    }
  ],
  "blocks": [
    {
      "type": "method_card",
      "name": "Method Card",
      "settings": [
        {
          "type": "text",
          "id": "card_title",
          "label": "Card Title",
          "default": "Fused Deposition Modeling (FDM)"
        },
        {
          "type": "textarea",
          "id": "card_description",
          "label": "Card Description",
          "default": "Mit dem FDM-Druckverfahren lassen sich zahlreiche Kunststoffe drucken. Neben einer hohen Materialauswahl zeichnet sich der FDM-Druck außerdem durch eine hohe Stabilität aus. In den meisten Fällen können keine großen/langen Teile produziert werden. Durch unsere Ausstattung ist dies allerdings möglich.Insgesamt ist das FDM-Verfahren sehr preiswert – ein weiterer Vorteil, den wir direkt an unsere Kunden weitergeben."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "3D Printing Methods",
      "blocks": [
        {
          "type": "method_card",
          "settings": {
            "card_title": "Fused Deposition Modeling (FDM)",
            "card_description": "Mit dem FDM-Druckverfahren lassen sich zahlreiche Kunststoffe drucken. Neben einer hohen Materialauswahl zeichnet sich der FDM-Druck außerdem durch eine hohe Stabilität aus. In den meisten Fällen können keine großen/langen Teile produziert werden. Durch unsere Ausstattung ist dies allerdings möglich.Insgesamt ist das FDM-Verfahren sehr preiswert – ein weiterer Vorteil, den wir direkt an unsere Kunden weitergeben."
          }
        },
        {
          "type": "method_card",
          "settings": {
            "card_title": "Stereolithography (SLA)",
            "card_description": "Beim SLA-Verfahren wird für ein Modell ein bestimmtes Kunstharz verwendet, das durch eine Lichtquelle ausgehärtet wird.Der Vorteil gegenüber anderen Druckvarianten zeigt sich darin, dass beim 3D-Druck auf allen Seiten eine Hochglanzoberfläche erzeugt werden kann und eine hohe Detailgenauigkeit beim Druck entsteht."
          }
        }
      ]
    }
  ]
}
{% endschema %}